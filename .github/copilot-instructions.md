# Copilot Workspace Instructions

- Always use the project-local Python environment at `.venv`.
- Never use Python interpreters from outside this repository (for example `~/Documents/maxrf_venv`).
- For terminal Python commands, use `${workspaceFolder}/.venv/bin/python`.
- If a Python environment must be configured, select `${workspaceFolder}/.venv/bin/python`.
